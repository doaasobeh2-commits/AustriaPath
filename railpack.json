{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "caches": {
    "npm": {
      "directory": "/tmp/.npm",
      "type": "shared"
    }
  },
  "steps": {
    "install": {
      "caches": ["npm"],
      "commands": [
        {
          "cmd": "npm ci --cache /tmp/.npm --no-audit --fund=false",
          "customName": "Install dependencies"
        }
      ]
    },
    "build": {
      "caches": [],
      "commands": []
    }
  },
  "deploy": {
    "startCommand": "npm run server:migrate && npm run server:start"
  }
}
